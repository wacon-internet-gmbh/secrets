plugin.tx_secrets_secret {
    view {
        templateRootPaths.0 = EXT:secrets/Resources/Private/Templates/
        templateRootPaths.1 = {$plugin.tx_secrets_secret.view.templateRootPath}
        partialRootPaths.0 = EXT:secrets/Resources/Private/Partials/
        partialRootPaths.1 = {$plugin.tx_secrets_secret.view.partialRootPath}
        layoutRootPaths.0 = EXT:secrets/Resources/Private/Layouts/
        layoutRootPaths.1 = {$plugin.tx_secrets_secret.view.layoutRootPath}
    }
    persistence {
        storagePid = {$plugin.tx_secrets_secret.persistence.storagePid}
        #recursive = 1
    }
    features {
        #skipDefaultArguments = 1
        # if set to 1, the enable fields are ignored in BE context
        ignoreAllEnableFieldsInBe = 0
    }
    mvc {
        #callDefaultActionIfActionCantBeResolved = 1
    }
}

# these classes are only used in auto-generated templates
plugin.tx_secrets._CSS_DEFAULT_STYLE (
    textarea.f3-form-error {
        background-color:#FF9F9F;
        border: 1px #FF0000 solid;
    }

    input.f3-form-error {
        background-color:#FF9F9F;
        border: 1px #FF0000 solid;
    }

    .tx-secrets table {
        border-collapse:separate;
        border-spacing:10px;
    }

    .tx-secrets table th {
        font-weight:bold;
    }

    .tx-secrets table td {
        vertical-align:top;
    }

    .typo3-messages .message-error {
        color:red;
    }

    .typo3-messages .message-ok {
        color:green;
    }
)

# Module configuration
module.tx_secrets_tools_secretssecret {
    persistence {
        storagePid = {$module.tx_secrets_secret.persistence.storagePid}
    }
    view {
        templateRootPaths.0 = EXT:secrets/Resources/Private/Backend/Templates/
        templateRootPaths.1 = {$module.tx_secrets_secret.view.templateRootPath}
        partialRootPaths.0 = EXT:secrets/Resources/Private/Backend/Partials/
        partialRootPaths.1 = {$module.tx_secrets_secret.view.partialRootPath}
        layoutRootPaths.0 = EXT:secrets/Resources/Private/Backend/Layouts/
        layoutRootPaths.1 = {$module.tx_secrets_secret.view.layoutRootPath}
    }
}
